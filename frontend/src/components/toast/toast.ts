/*import { toast } from 'react-toastify';

export const showToast = (message: string, type: 'success' | 'error' | 'info' | 'warning') => {
  toast(message, {
    type,
    className: `toast-${type}`, // Use custom classes for different types
    style: {
      backgroundColor: type === 'success' ? '#4caf50' :
                       type === 'error' ? '#f44336' :
                       type === 'info' ? '#2196f3' :
                       '#ff9800',
      color: '#fff'
    }
  });
};*/
